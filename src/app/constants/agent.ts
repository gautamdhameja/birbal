// Purpose: Collects shared agent constants.
// Scope: Avoids scattering repeated literals across runtime modules.

import { FRAMEWORK_AGENT } from "../../framework/agent/constants.js";

export const AGENT = FRAMEWORK_AGENT;
